my_profile:  # Name of your profile
  target: dev  # The default target to use
  outputs:
    dev:
      type: snowflake
      account: qia75894 # Snowflake account (e.g., xy12345.us-east-1)
      user: atlanadmin  # Snowflake user
      password: rhg@DCH.xuz7jcy1bmk  # Snowflake password
      role: TRANSFORMER  # Snowflake role (optional)
      database: ANALYTICS  # Snowflake database
      warehouse: TRANSFORMING  # Snowflake warehouse
      schema: PUBLIC  # Snowflake schema
      threads: 1  # Number of threads to use for parallel execution
      client_session_keep_alive: False  # Keeps the connection alive (optional)
